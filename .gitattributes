# Normalize line endings; the CI runs on Linux.
* text=auto

# The Gradle wrapper script must stay LF or it breaks on Linux/macOS.
gradlew text eol=lf
*.sh text eol=lf

# Windows batch files need CRLF.
*.bat text eol=crlf

# Binary files.
*.png binary
*.jar binary
